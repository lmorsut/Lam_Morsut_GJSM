Could not locate original runtime file, so this is the saved copy CC3D makes when it runs the runtime.
It is identical to the original, but may start the parameter scan at seed #2 rather than seed #1. 
Just change the number of runs you desire by adding/removing parameter scan seed values.